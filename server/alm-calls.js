// place holder for logic

/**
 * @todo Sign on to instance
 * @body Program how we will log on to the servers. Return the sso cookie as the call back (or the full response; iunno really)
 */
function signOn (almUrl, credentials, callback) {
  // We must strategize beep boop..for Real
}

/**
 * @todo Write the logic for performing the READ calls for specific items (defects to start)
 * @body Most common uses cases are to read defects and then comment on them. This will handle just this work flow, 
 * starting with GETting the defect and reading it.
 */
function getInstance () {
  // We must strategize beep boop..for Real
}

